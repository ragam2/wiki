image: python:3.7.3
lint:
  script:
  - pip install flake8==3.9.1
  - flake8 .
test:
  script:
  - pip install -r requirements.txt
  - pytest .
format:
  script:
  - pip install black==21.4b2
  - black --check .
